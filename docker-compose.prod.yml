services:
  web:
    environment:
      - NODE_ENV=production
    command: ["node", "server.js"]

  api:
    environment:
      - APP_ENV=production
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "2"]
